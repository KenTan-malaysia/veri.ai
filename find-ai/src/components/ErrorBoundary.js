// v3.7.6 — DELETED. Replaced by Next.js App Router /app/error.js global error boundary.
// Was imported by /app/page.js but never actually rendered (orphan import).
// Original code preserved in git history. Physically delete with: Remove-Item "src/components/ErrorBoundary.js"
