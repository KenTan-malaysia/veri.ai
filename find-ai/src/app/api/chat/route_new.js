// v3.7.4 — DELETED FILE (kept as tombstone until next git rm).
// This was an orphan duplicate of src/app/api/chat/route.js. Zero callers.
// Next.js ignores files that export no HTTP method handlers, so this is
// safe to leave but should be physically deleted on the next opportunity:
//   Remove-Item "src/app/api/chat/route_new.js"
