// v3.7.4 — DELETED FILE (kept as tombstone until next git rm).
// This was the email-collection endpoint for the landing Audit teaser, which
// was retired in v3.5.4 when /audit shipped live. Zero callers since.
// Safe to leave but should be physically deleted:
//   Remove-Item -Recurse "src/app/api/notify-me"
